export function NotebookEditor({ onClose }: { path?: string; root?: string; onClose: () => void; controls?: React.ReactNode }) {
  return <div className="p-4 text-sm text-muted">Notebook editor coming soon.</div>;
}
