{
  "name": "@pi-science/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node scripts/copy-kernel-bridges.mjs && node scripts/copy-sqlite-assets.mjs",
    "dev": "tsx watch src/app/main.ts",
    "start": "node dist/app/main.js",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@fastify/http-proxy": "^11.6.0",
    "@pi-science/contracts": "workspace:*",
    "fastify": "^5.10.0",
    "jszip": "^3.10.1",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
